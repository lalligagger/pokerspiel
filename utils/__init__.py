"""Utilities for reduced-flop benchmarking and board canonicalization.

This module intentionally keeps experimental logic isolated from the main solver
service layer so the production run path remains stable while PoC features are
being evaluated.
"""
