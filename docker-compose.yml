services:
  pokerkit-open-spiel:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    environment:
      CONFIG_PATH: /app/cfg/solve_config_local.json
    entrypoint: ["/bin/bash", "/app/docker-entrypoint.sh"]
